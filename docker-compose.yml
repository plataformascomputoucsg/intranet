version: '3'
services:
  next-app:
    container_name: nextjs-app
    build:
      context: .
      dockerfile: Dockerfile
      # Pasamos las variables PÚBLICAS para que se quemen en el build
      args:
        NEXT_PUBLIC_MESA_AYUDA_URL: ${NEXT_PUBLIC_MESA_AYUDA_URL}
        NEXT_PUBLIC_SERVICIOS_ADMIN_URL: ${NEXT_PUBLIC_SERVICIOS_ADMIN_URL}
        NEXT_PUBLIC_SIU_URL: ${NEXT_PUBLIC_SIU_URL}
        NEXT_PUBLIC_SIU2_URL: ${NEXT_PUBLIC_SIU2_URL}
<<<<<<< HEAD
        NEXT_PUBLIC_GESTOR_DOCUMENTAL_URL: ${NEXT_PUBLIC_GESTOR_DOCUMENTAL_URL}
        NEXT_PUBLIC_CAMBIO_CLAVE_URL: ${NEXT_PUBLIC_CAMBIO_CLAVE_URL}
        NEXT_PUBLIC_TRANSPARENCIA_URL: ${NEXT_PUBLIC_TRANSPARENCIA_URL}
        NEXT_PUBLIC_DESCARGA_DOCUMENTOS_URL: ${NEXT_PUBLIC_DESCARGA_DOCUMENTOS_URL}
        NEXT_PUBLIC_MANUALES_URL: ${NEXT_PUBLIC_MANUALES_URL}
        NEXT_PUBLIC_SAC_URL: ${NEXT_PUBLIC_SAC_URL}
        NEXT_PUBLIC_BRIEF_DCM_URL: ${NEXT_PUBLIC_BRIEF_DCM_URL}
=======
>>>>>>> 407c292b6ab4b2024c4b0119e4fbe99f1e95480e

    ports:
      - '3000:3000'

    # Pasamos las variables PRIVADAS (Runtime) como API_URL
    env_file:
      - .env

    # Permite que el contenedor resuelva 'intranet' igual que el host Windows
    extra_hosts:
      - 'intranet:172.16.1.18'
